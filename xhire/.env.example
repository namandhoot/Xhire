# XHire API endpoint (recommended)
# Request access to the centralized API service
VITE_XHIRE_API_ENDPOINT=https://your-xhire-api-endpoint.com/api

# --- OR ---

# Twitter API (X) credentials
# Get your token at https://developer.twitter.com
VITE_TWITTER_BEARER_TOKEN=your_twitter_bearer_token_here

# Google Gemini API key
# Get your key at https://ai.google.dev
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Enable using owner's API keys (recommended)
# This ensures users don't need to provide their own API keys
VITE_USE_OWNER_KEYS=true

# Note: Configure either the XHire API endpoint OR the Twitter/Gemini API keys
# If nothing is configured, the application will use mock data 